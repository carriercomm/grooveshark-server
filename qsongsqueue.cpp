#include "qsongsqueue.h"

QSongsQueue::QSongsQueue() {

}

/**
 * Accepts a JSON encoded string in the form of QByteArray.
 * Once decoded successfully, the songs queue will get updated with
 * SongIDs.
 *
 * @param  data JSON string
 * @return
 */
bool setJson(const QByteArray & data) {

    return true;
}
